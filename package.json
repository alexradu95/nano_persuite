{
  "name": "persuite",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
    "scripts": {
    "dev": "bun --hot run server.ts",
    "start": "bun run server.ts",
    "db:reset": "rm -f personal.db && bun run server.ts"
  }
}
